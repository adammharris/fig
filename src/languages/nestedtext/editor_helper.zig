//! NestedText in-place editing support. Placeholder for now — see the
//! project plan (`fig-nestedtext-support` memory note): wiring `Editor(NestedText)`
//! into `src/editor.zig`'s comptime-branched engine is a separate pass from
//! the reader/printer/conformance work landed so far.
